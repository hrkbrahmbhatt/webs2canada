<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hrsikeshbrahmbhatt.webs2canada">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionbar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home"
            android:label="Home"></activity>
        <activity android:name=".OurService"
            android:label="Services"></activity>
        <activity android:name=".Team"
            android:label="Team"></activity>
        <activity android:name=".OurPort"
            android:label="Portfolio"></activity>
        <activity android:name=".Contact"
            android:label="Contact us"></activity>
        <activity android:name=".Price"
            android:label="Pricing"></activity>
        <activity android:name=".OurLocation"
            android:label="Location"></activity>
        <activity android:name=".OurFacebook"
            android:label="Facebook"></activity>
        <activity android:name=".OurLinkedin"
            android:label="Linkedin"></activity>


    </application>

</manifest>